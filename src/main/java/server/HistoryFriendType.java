package server;

/**
 * Used by {@link History} to determine if the friend request made was a send request or accept.
 * @author Robbie Booth
 */
public enum HistoryFriendType {
    SEND,
    ACCEPT,
}
